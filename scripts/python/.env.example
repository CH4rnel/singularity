TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
DEPLOYER_PK=
HTTP_PROXY=
X_USERNAME=
X_EMAIL=
X_PASSWORD=
X_MONITOR_ACCOUNT=cyberia_chain

# Lending comptroller address (same value as the DEX frontend's VITE_LENDING_COMPTROLLER).
# Required to enable the supply/withdraw/borrow/repay announcer; leave empty to disable it.
LENDING_COMPTROLLER=0xe66aa9842dc74F1c10ede19cA20Ece6E08F1CC88

# Whales chat the /whale gate invites verified CYBER.sol holders into.
# MUST be the numeric chat id (supergroups look like -1002688848155, NOT 2688848155),
# and the bot has to be a member of that chat and an admin with "Invite Users via Link".
# Leave empty to disable the whale gate entirely.
WHALE_CHAT_ID=